const DFDivider = ({ className = "" }) => {

    return (

        <hr
            className={`border-0 border-t border-[var(--df-border)] ${className}`}
        />

    );

};

export default DFDivider;